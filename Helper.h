//Header.h Contains the declarations of methods that check parameters and contents of strings (make lowercase)
//Authors: F Huang 

#pragma once
#include<string>
#include<vector>
using namespace std;

int usage_message(char * program_name_input, char * usage_info);
string to_lowercase(string content);
